{
  "name": "@pytest-aitest/templates",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind CSS build for pytest-aitest HTML reports",
  "scripts": {
    "build": "tailwindcss -i input.css -o partials/tailwind.css --minify",
    "watch": "tailwindcss -i input.css -o partials/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^3.4.19"
  }
}
